"""Configured context models, dynamic values and data-driven content.

This package resolves project-declared models (``WAGTAIL_DAISIE_CONTEXT_MODELS``)
into page/block contexts and provides the dynamic image/link primitives that
consume them. Importing it never touches the database.
"""
